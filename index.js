const name = "Susan";  // Changed this line to set name equal to "Susan"
const height = 30;  // Set height to a value less than 40
const message = `${name} is ${height} inches tall`;  // This provides a meaningful message

module.exports = { name, height, message };
